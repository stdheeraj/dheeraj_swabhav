create table Dependent (pno            smallint,
                        dname          VARCHAR(20),
                        relationship   VARCHAR(8),
                        sex            char(1),
                        age			smallint)