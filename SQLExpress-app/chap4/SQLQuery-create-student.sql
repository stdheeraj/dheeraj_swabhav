create table Student (stno            smallint,
                        sname          VARCHAR(20),
                        major          VARCHAR(4),
                        class            smallint,
                        bdate			smalldatetime)